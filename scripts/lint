#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "==> ruff check"
ruff check .

echo "==> ruff format check"
ruff format --check .

echo "==> mypy"
mypy custom_components/proconip_pool_controller
